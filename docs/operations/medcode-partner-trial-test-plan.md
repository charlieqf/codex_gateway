# Former MedCode Partner Trial Test Plan

Status: archived.

This checklist targeted the retired Azure endpoint and public model id
`medcode`. It must not be used as a current acceptance contract.

Current public Gateway facts are in [System Status](./system-status.md). Use the
current origin, current model ids and the task-specific smoke named by the
[Runbook Index](./runbook-index.md). Always capture `X-Request-Id`, keep tool
execution client-side, and redact credentials, private source, PHI and private
filesystem content from reports.

The former MedEvidence, OpenCode CLI and Desktop trial cases remain in Git
history if a historical comparison is explicitly required.
