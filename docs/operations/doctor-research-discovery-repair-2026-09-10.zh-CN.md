# Doctor Research 检索与恢复修复（2026-09-10）

**验收结论更正：下文的成功回归不能证明通用性。** 虽然请求没有手工提供个人官网 URL，但已发布代码包含调查这些案例时新增的机构译名/官网、指定专业网站信任及搜索规则，以及按姓名生成特定网站个人页的规则。这些属于案例相关的提前准备。移除这些新增预置后，原六例身份识别仅 2/6 通过，海外四例均未通过；加测王永生也未通过。详见 [通用性审计](./doctor-research-generalization-audit-2026-09-10.zh-CN.md)。

用户提供的六例均使用原始中文机构、科室或职务输入；加测王永生原问题。修复覆盖 Research Worker 和 Research Agent，Gateway API 请求格式保持兼容。生产当前仍为含上述预置的 `8efd541`；后续格式修复 `645b35c` 和去预置对照 `a67d2d3` 均未发布。

## 原因与处理

- SerpAPI 请求错误被通用异常覆盖，丢失 timeout、HTTP 状态及可重试性质；搜索阶段尚未调用模型的失败又被排除在恢复条件之外。现在保留类型，记录脱敏的请求耗时、状态、query hash、search ID；局部失败不再停止整个 worker。历史王永生任务的原始异常已丢失，只能确认搜索阶段失败，不能断言当时 Google 没有结果或供应商宕机。
- 首轮候选存在不代表身份已核实。改为抓取正文后判断是否补搜；补充机构译名、科室译名、医院站内真实导航和经审阅的专业机构来源，并行抓取三路，证据足够即取消多余抓取。仍要求正文同时支持姓名、机构及专业；403、空壳页面不会成为证据。
- 中外混合输入也用于 PubMed 时，以经审阅的外文机构与专业别名参与检索及作者所属单位核验。行政职务中的妇产、母胎等临床方向可用于身份确认，具体“副院长”等任职必须另有来源支持。
- 成功但零结果、供应商处理中、异常响应分别分类。补搜及网页抓取有全局上限，既有请求总上限 1000、响应字节上限 2 GB、570 秒截止时间不提高；生产配置需从 964/1.928 GB 调整到现有上限以覆盖预算预留。
- Axel 完整回归暴露模型在 JSON 字符串中输出 Markdown `\-` 的格式错误，且单次模型修正重复了错误。增加只保留 Markdown 原文的反斜杠编码修复；不推断缺失字段、括号或截断内容。
- 报告明确区分检索输入和已核实任职，标明来源列表日期为访问日期；企业、协会人员不因进入本服务而自动被称为临床医生。文献未查到不等于本人没有论文。

## 真实联网回归

在 R760 隔离容器中调用实际 SerpAPI、网页、PubMed/Crossref 和内部 Research 模型；生产数据库只读，任务及产物写入临时 SQLite。下表使用含案例相关预置的版本，是修复迭代中各例的成功运行记录，并非冻结代码后一次连续全通过的盲测。包含供应商缓存及模型耗时，不是冷缓存性能承诺。此前失败和公网失败另行保留，不能计为成功。

| 输入 | 机构 | 完整流程 | 耗时 |
| --- | --- | --- | --- |
| 沈嵘 | 南京市妇幼保健院 | succeeded，4 份产物 | 33.886 秒 |
| 漆洪波 | 重庆医科大学附属第一医院 | succeeded，4 份产物 | 31.053 秒 |
| Leonhard Schaetz | 诺华 | succeeded，4 份产物 | 58.801 秒 |
| Axel Rominger | 瑞士伯尔尼大学小岛医院 | succeeded，4 份产物 | 53.548 秒 |
| Riemer Slart | 荷兰格罗宁根大学医学中心 | succeeded，4 份产物 | 68.839 秒 |
| Iva Hristova | 欧洲核医学协会 | succeeded，4 份产物 | 40.862 秒 |
| 王永生 | 四川大学华西医院 | succeeded，4 份产物 | 61.218 秒 |

28 份产物全部通过实际文件大小和 SHA-256 校验。Leonhard、Axel、Riemer 已检索并核实本人论文；Iva 使用 RSNA 2021 年一手报道，职务信息以当年来源为准。漆洪波可由政府网站的一手专业报道确认身份，但不能将输入中的副院长直接视为该篇报道已证明的现职。简报仍可能带来源/论文覆盖不足及既有 brief 模式质量提示，完整执行成功不等于完整履历或严格全文综述。

## 验证和部署边界

自动化覆盖真实截图输入、错误医院/科室及聚合站反例、站内导航、抓取取消、补搜、成功零结果、超时/传输/鉴权分类、外文论文归属、格式修复和连续任务恢复。真实 worker runtime 测试验证搜索连续两次超时后下一任务仍成功、心跳恢复 ready。

部署须从本分支已提交、测试、推送的 revision 生成源码归档，叠加两个组件的构建产物到已核实的原 Worker 基础镜像。只重建 research-worker，保留既有 Gateway current/previous 与其他服务实例。部署前创建并验证 Research SQLite 和配置备份；部署后检查镜像 revision、心跳、健康、重启数、公开 API 和产物校验。原用户的历史失败任务不由探针修改。

## 已发布版本

2026-09-10 11:34 UTC，R760 Research Worker 已更新为提交 `8efd541caea150e5ffb6dea59a8b4f354a19abd8`，镜像 ID `sha256:57cbb4ad3efa52305cc4b86853f4cfb88eda534db372b178b2acf47951aa77be`，心跳版本 `research-repair-8efd541caea1`。提交后的 TypeScript 构建通过；Research Agent、Worker 和 Gateway Research 路由共 **289 项测试通过**。

- 只重建 `research-worker`；Gateway 容器仍为 `143c1445aef8`，`current=8dab89da424ce722df2c433d52132e19707536b9`、`previous=31946c97954af05399582010f5da7589204aac4b`。
- 复用容器标签确认的原 `ebad087` 两个 Compose 文件、共享 R760 override 和生产 env 文件；共享 override 中单独固定 Worker 镜像、版本及预算。
- 已验证备份：`/opt/codex-gateway-r760/backups/doctor-research-repair-8efd541caea150e5ffb6dea59a8b4f354a19abd8`。Research SQLite 快照、配置副本和发布元数据均受权限保护；数据库 `quick_check=ok`、外键违规 0。
- 源码归档 SHA-256：`1f89a06b40d1dcc5b236c4e7bd6ca9bc86e5ac77d698ec11738b03b1cde8dcde`；两个组件构建归档：`af61867bd28dc0d1806dd8aa7a355e8698ff99709acfdd8a52357e9e79aa476f`。
- 回滚须只处理 Worker 配置及旧镜像 `sha256:d97bfc98b8751082f9ca1337f133c1df04e41af16c33ae2cb1216ca33b79e136`，使用相同 Compose 组合。共享 override 如已有其他后续变更，不可整文件覆盖。Gateway 的 current/previous 不承担本次 Worker 回滚。

公网验收使用临时 Research 测试权益。首次测试脚本默认 `code` scope，与 Research Plan 的 `medical` scope 不符，权益配置返回 409，未创建 Research 任务；脚本已禁用该临时账号并撤销其两类凭证。改用正确 `medical` scope 后成功创建独立测试账号。此错误属于测试准备，不计入搜索回归成功率。

公网实际完成 7 个任务，6 个成功、24 份产物校验通过；Riemer Slart 的 `drr_7bc6369a37bf41669979d0311366a77f` 身份识别成功，但两次模型响应均发生 `model_contract_error`，不能宣称公网七例全部通过。后来隔离复现另一次响应存在多余终止 `}`，`645b35c` 可解析该留存响应；公网未留存原始模型文本，不能断言两次错误语法相同。该后续修复尚未发布。
