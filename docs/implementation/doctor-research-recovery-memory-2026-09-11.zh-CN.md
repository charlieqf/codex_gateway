# Doctor Research 生成恢复与调查笔记（2026-09-11）

状态：实施中，未部署。所有原始医生的完整服务验收仍未完成。承接[证据 Agent 记录](doctor-research-evidence-agent-2026-09-11.zh-CN.md)。真实原始输入试跑与诊断回放分开统计，不能用回放替代无预设案例验收。

当前三段 Agent 通路的显式总模型预算为 29 次。第七、八次回放完成文件交付，但人工抽查发现内容漏审；后续试跑暴露了输入/输出容量、整份提案反复重写和来源工作区丢失上下文的问题，均未完成验收。沈嵘最新原始输入试跑已通过身份复核，在文献阶段失败。前文配置按当时状态保留，最新回归和演进见后文。

## 最新：文献工作区与完整摘要（23:13 UTC 后）

### canary9 与批量校验反馈（23:25 UTC 后）

[canary9](../../artifacts/doctor-research-agent-2026-09-10/workflow-canary9/result.json)冻结提交 `1d16fae402ed76bdcc05bad0ae92b640dfe983cc`。沈嵘原始输入的身份用 4 次模型调用、约 15 秒通过；文献 Agent 使用来源工作区，并在第 4 次调用提交提案，未重现上一轮六次交替读取的循环。但首次提案 JSON 语法错误，下一次重写耗时 83.147 秒。随后校验每次只报告第一个问题，连续暴露三个不同来源的引句问题和作者单位缺失。Agent 确实读回缺失的作者单位并提出修复，但最后三次补丁把路径写成 `/evidence/doctorPublications/0`，旧接口只接受 `/doctorPublications/0`，笼统错误未帮助其收敛。

整项 **300.138 秒**、模型 16 次、搜索 HTTP 1 次、文件 0 个，文献独立复核仍未到达。搜索命中既有服务商缓存，仅 163 毫秒；模型调用均无准入等待。耗时主要在模型提案及纠错，不能解释成搜索引擎搜不到，也不能把失败任务归为可接受服务。

后续通用修复一次收集各条事实、本人论文、领域论文、核心证据及研究范围的校验问题，附记录路径，最多反馈 24 条；任何记录无效都不能部分送审或交付。补丁允许无歧义的 `/evidence/` 外层前缀，先规范化再检查路径存在、重叠和原型属性；不猜字段，不修改事实，也不绕过当前提案哈希。路径错误明确指出实际失败路径。构建通过，[相关回归 402/402](../../artifacts/doctor-research-agent-2026-09-10/regression-batched-evidence-feedback-20260911.json)，新增覆盖多条错误同轮反馈、完整修复后才复核，以及两种路径写法造成重叠时的原子拒绝。它们晚于 canary9 冻结，真实效果待下一轮验证。

当前模型链没有传递 `response_format` 约束：Research 客户端只提交普通消息，Gateway Chat 解析和兼容提供商请求体也没有该字段。结构化输出能力需要贯通、确认提供商支持后才能使用；本轮未启用或假定其有效，也未用自动改写 JSON 内容代替语义审核。

导出包 SHA-256 `8693642bbdf3a89786d469d5130a1bc124ade3581fe309bcd7d062de78528a55` 已验证并[清理](../../artifacts/doctor-research-agent-2026-09-10/cleanup-canary9-20260911.json)。[免费账户查询](../../artifacts/doctor-research-agent-2026-09-10/serpapi-balance-after-canary9-20260911.json)在 23:25:00 UTC 仍为剩余 42、月已用 208。已归档 26 项探针、9 次搜索 HTTP 请求；15 位完整原始案例验收仍全部待完成。

[canary8](../../artifacts/doctor-research-agent-2026-09-10/workflow-canary8/result.json)冻结提交 `b2950dd97531f342e19436b57106871c72be16c6`，仍只输入沈嵘的原始三个字段。耗时 **174.792 秒**，身份 6 次、文献 12 次模型调用，搜索 HTTP 请求 1 次，无文件；不能因低于 5 分钟就归为优秀。身份核验通过，随后读取 24 篇论文，但文献 Agent 后六次反复交替读取已缓存的作者单位与核心摘要，没有提交证据提案。滚动工具窗口只留 12 条观察，前一组原文会被后一组挤出；笔记虽保留了计划，却没有保留待引用原文。这轮没有引句错误，不能继续归因于前一轮引用校验缺陷。

因此新增由 Agent 选择的 `focusPublications` 来源工作区：保留最多 16 篇已成功读取论文的完整、摘要或作者归属视图，跨后续调用和同任务恢复持续提供实际来源。它不保存预置人物答案，笔记也不能代替来源。无效或超过上下文容量的选择原子拒绝，保留旧选择。重复单位文本只存一份，各作者单位顺序及本地索引保留；未分配到作者的汇总单位单独保留，不建立虚构归属。接近调用上限时明确提示还需给证据提案和独立复核各留一次机会。

同时修复完整 Agent 通路在进入最终写作/复核前仍调用旧摘要压缩函数的问题。最终来源核查与整稿复核现在收到每篇选定摘要的完整文本，容量不足时显式失败，不能省略中间内容却声称完整审阅。合成 Worker 测试把一条限制放在约 1.8 万字符摘要中间，验证两类复核均能收到。

构建通过，[相关回归 399/399](../../artifacts/doctor-research-agent-2026-09-10/regression-focused-evidence-20260911.json)，包括来源被滚动窗口挤出后的恢复、不重复联网、作者单位顺序与未归属单位保留，以及无效/超大选择的原子拒绝。这些测试验证机制；两项改动尚待新的真实模型试跑。[源码有限核查](../../artifacts/doctor-research-agent-2026-09-10/generic-runtime-source-audit-focused-evidence-20260911.json)未发现新增已知案例姓名或机构原字符串，仍不能替代无预设完整案例验收。canary8 导出 SHA-256 `88cd6bbec501bad26972effb02ad016c487280e99aaa2034ab5c7eb0ddbc205b` 已校验、扫描并[清理远端临时目录](../../artifacts/doctor-research-agent-2026-09-10/cleanup-canary8-20260911.json)。[免费账户查询](../../artifacts/doctor-research-agent-2026-09-10/serpapi-balance-after-canary8-20260911.json)在 23:11:33 UTC 确认仍剩 42 次，月已用 208；本轮搜索返回既有服务商缓存。历史已记录 25 项探针、8 次搜索 HTTP 请求，不能将 HTTP 请求数直接当作计费数。

## 本次真实原始输入试跑

[canary3](../../artifacts/doctor-research-agent-2026-09-10/workflow-canary3/result.json)仅输入 Shoshana Sztal-Mazer / 阿尔弗雷德医院 / 内分泌科，未提供官方 URL、人物表或历史资料。实际 SerpAPI 请求为 1 次，41.626 秒后返回成功；旧的 20 秒等待预算会截断这样的请求。随后 Agent 读取检索到的公开页面并通过独立身份复核，证明本次中文机构和科室输入可经实际网页证据核验。

流程在资料与文献阶段读取 10 篇论文后，第三次证据模型调用遇到上游超时，总耗时 135.284 秒，未交付任何文件。私有模型 Gateway 的对应请求 `req-5e9a25ac-1c36-4dcc-a1c9-54cff88e58ee` 返回 504，记录 `upstream_timeout`，耗时约 50 秒。此时 Worker 为该调用配置 60 秒阶段预算，其中约 50 秒分配给模型执行。后续将证据分析调用预算改为 120 秒，仍受整项硬截止时间约束。

本次并非“搜不到医生”，也不是完整服务成功。2026-09-10 15:46:45 UTC 的免费 Account API 查询返回剩余 43 次、月已用 207 次；本轮诊断回放不会调用 SerpAPI。

## 通用改动

- **生成阶段恢复**：身份和证据状态以外，已完成的正文生成与输出复核响应也保存到所属任务的租约保护状态。复用键包含模型、阶段、调用序号、系统提示、任务提示、输出预算和推理配置。仅同一任务、同一调用内容可复用；不同调用序号仍可重新生成。外部调用前继续扣除持久化预算，恢复不会重置已用预算。复用内容仍经过完整质量检查和最终提交的租约校验。
- **调查笔记**：Agent 可自行保存带来源/PMID 的临时计划和筛选笔记，后续决策继续读取。它们不是独立证据，不能替代实际原文引用与语义复核。该机制用于减少较早工具结果离开工作窗口后的重复读取，没有医生专用内容。
- **参考文献目标**：调查阶段直接接收医学合同的 40 篇目标；3 篇仅是安全下限。未达目标须说明实际证据或资源限制，复核模型检查解释与真实查询、读取记录是否一致；不允许用无关文献凑数。
- **复核输入**：保留所选论文的完整摘要。本人论文额外提供作者名单及已选作者本人的单位记录，不重复所有合作者单位和无关书目元数据。原始完整记录仍保存在任务状态中。
- **调用序号**：模型客户端的旧 `attempt <= 9` 与新的 Agent 循环不兼容，已与存储层对齐到 1–100；实际调用仍受调查和整项持久化资源预算约束。默认证据循环改为最多 12 次模型调用，覆盖调查、格式纠正和独立复核；身份 8 + 证据 12 + 生成/复核 6，共需显式配置 26 次总预算。本次探针的每次输入上限 38000 token、总输入 1000000、总输出 300000。
- **格式纠错**：错误指向具体字段和来源 ID。仅当 `evidence.limitations` 缺失而顶层存在唯一的 `limitations` 数组时，机械移动原数组并保留修复记录；不改写其内容，也不放宽事实引用。插入省略号或翻译后的“原文引句”仍须由 Agent 纠正。
- **选题证据**：选题仍须包含官方职责页面或已核实本人论文的依据；同时允许引用实际读取的领域论文解释综述主题。不能仅因补充引用属于其他作者就机械拒绝；独立复核继续判断主题关联，也不能把领域进展写成本人研究成果。

完整报告诊断使用已有的三段并行生成能力，后续仍须独立复核。该设置目前只在隔离探针中明确启用，不能据此声称生产配置已更新或真实服务耗时已达标。

## 验证范围

[恢复回归](../../artifacts/doctor-research-agent-2026-09-10/regression-recovery-20260911.json)为 341/341；新增调用序号和格式修复后，[最新相关回归](../../artifacts/doctor-research-agent-2026-09-10/regression-recovery-memory-20260911.json)为 342/342，TypeScript 构建通过。恢复集成测试在正文完成、复核请求发生 503 后真实重新排队并获取下一代租约，确认正文与此前身份搜索没有重做，最后仍提交四种产物。该测试用合成资料和简化内容合同，验证恢复机制，不代表真实医学报告质量验收。

诊断回放以 canary3 自己产生的身份和证据状态为输入，付费搜索硬上限为 0，显式记录 `diagnostic_replay=true` 和 `fresh_case_acceptance_eligible=false`。回放会创建新的隔离诊断任务，因此其耗时不能当作原任务从提交到交付的总耗时。

第一次回放继续读取到 30 篇论文，在第 9 次证据调用返回提议后遇到字段位置错误；第 10 次调用又被模型客户端的旧序号上限在本地拒绝。共尝试 7 次模型调用，其中 6 次抵达模型，最后一次在本地校验即失败。总耗时 192.274 秒，没有生成报告。此次失败推动了上述序号上限、调查笔记、具体错误说明和格式修复改动，保留失败证据，不算案例通过。

## 超过八分钟的回放调查

[第二次回放](../../artifacts/doctor-research-agent-2026-09-10/workflow-replay2/result.json)耗时 **550.097 秒（9 分 10 秒）**，搜索 0 次、模型调用 13 次，没有交付文件。按用户标准必须调查。资料阶段读取 40 篇论文，选入 17 篇领域参考文献、3 篇本人论文和 8 条核心证据，并通过独立语义复核；40 篇参考文献目标未达，保留限制说明。

可观测的主要耗时如下，串并行阶段不能直接将所有调用时长相加：

| 环节 | 耗时与证据 |
| --- | --- |
| 从恢复证据到开始生成 | 约 251.7 秒，其中证据提议两次分别 103.4、64.6 秒；全部证据模型调用累计 211.7 秒 |
| 首轮三段生成 | 单次约 70.5、70.7、119.8 秒；第三段较晚启动，整体墙钟约 134 秒 |
| 旧分支重写片段 | 顺序执行两次，合计约 82.3 秒 |
| 最终复核 | 等待约 81.6 秒后收到 504，`req-a2397126-8739-42f4-bc7b-203a8232cbe8` |

最后的 504 被旧的确定性回退分支吞掉，最终返回 `model_contract_error`，而没有保留暂时性依赖失败的恢复语义。报告也确有缺引文、混入问答、答案长度不足等问题，不能因为识别出误判就直接交付。

同时确认固定语义规则存在误判：实际 PubMed 摘要中写 `Seven RCTs`、`Twenty-five women`，对应数字被准确译成 7、25 时，逐字数字集合检查仍可能拒绝；同一段落中已有前文对象，后句的“该研究”仍可能被句首正则拒绝。这些判断需要读懂原文与上下文。

## 报告复核的职责调整

新增 [narrative-review-agent.ts](../../packages/research-agent/src/narrative-review-agent.ts)，在 `identityAgentEnabled=true` 且 `synthesisShardCount=3` 的完整报告通路启用。三段正文完成后，剩余生成预算直接交给复核 Agent，不再进入旧的逐项规则重写流程。单段旧通路仍保留，目前生产未启用新通路。

- Agent 读取所有所选论文的**完整摘要**和全局引用对应关系，核查数字、分母、单位、结局、因果、证据适用范围、综述连贯性及五组问答。启发式告警作为待核查线索，不能代替语义结论。
- 代码继续强制校验数据结构、引用 ID 范围、来源闭合、章节与字数合同、危险标记、调用预算、文件哈希和租约。模型批准不能越过这些检查。
- 修改仅允许针对明确列出的标题、摘要、段落、关键词、问题或答案目标；每个目标用原值 SHA-256 绑定。人物事实和已独立核验的核心证据表不可被这个阶段改写。修改后的候选必须在后续独立调用中重新审核，不能边修改边宣称验收通过。
- 服务负责渲染核心证据表和参考文献列表，提示明确说明这些内容不必重复写入正文。复核 Agent 可以直接完成有依据的修改，不能把可修复的问题交还给不存在的“作者环节”。
- 单个 JSON 代码围栏只作可逆格式展开；多个对象、额外说明、错误字段、陈旧哈希和不合法修改仍拒绝。上游错误直接传回持久化恢复层；上下文超限按资源不足分类，不裁剪证据后伪装复核完成。
- 协议类型由调用方固定并逐字段验证，不让模型填写的版本标签选择解析器。模型若多写了字符串 `schema_version`，将其作为无权威的冗余元数据忽略；仍严格检查其余决策字段、候选哈希与每条修改的原值哈希。这样不会因为版本标签拼错而重新生成数千字的有效修改。

[初轮相关回归](../../artifacts/doctor-research-agent-2026-09-10/regression-narrative-agent-20260911.json)为 360/360；补充围栏与上下文预算检查后，[最新回归](../../artifacts/doctor-research-agent-2026-09-10/regression-narrative-envelope-20260911.json)为 **363/363**，构建通过。Worker 集成测试还覆盖三段生成完成后复核 503、实际重新排队和租约恢复，确认搜索与三段生成没有重做；长度不合格时，即使模拟模型批准也不能交付。这些合成测试验证边界与恢复，不代表真实医学内容合格。

第三次零搜索回放复用第二次回放留下的身份、证据和生成响应，耗时 209.081 秒，新发 3 次复核模型请求。三次都返回包在 JSON 代码围栏中的提议，当时严格解析器均拒绝，因此没有交付；这推动了上述围栏兼容。它确实识别出数字表达等价与真实稿件缺陷，但不能算报告验收通过。

[第四次回放](../../artifacts/doctor-research-agent-2026-09-10/workflow-replay4/result.json)耗时 389.161 秒，新发 4 次复核模型请求、搜索 0 次。首次修改因版本标签拼写错误被当时的解析器拒绝；后续请求 `req-bfdb1d6c-dd59-4cfe-a2d7-d5ce17956fe8` 等待 110.023 秒后收到 504，任务实际重新排队、获取第二代租约，成功复用已完成的调查和正文。恢复后的调用还经历约 31 秒准入等待。最后一次调用完成合法修改，但剩余复核预算为零，按要求没有交付未经后续审核的稿件。这次证明了真实上游故障恢复，不能算完整输出通过。

另经[私有 Gateway 请求日志只读核对](../../artifacts/doctor-research-agent-2026-09-10/replay4-provider-effort.json)，上述三个请求均将 `requested_reasoning_effort=none` 按 `legacy_alias` 规范为 `effective_reasoning_effort=low`。因此不能把 Worker 的 `none` 参数解释为实际不产生推理 token。模型返回中也有推理 token 计数；其单独墙钟耗时并未测量，不将 token 占比直接换算为时间占比。本次没有修改生产模型、提供商或推理配置。

补充协议元数据处理后，[相关回归](../../artifacts/doctor-research-agent-2026-09-10/regression-narrative-protocol-20260911.json)为 **365/365**，构建通过。[第五次回放](../../artifacts/doctor-research-agent-2026-09-10/workflow-replay5/result.json)仍使用第二次回放的原始任务证据与生成响应，未加入人工整理的答案。其耗时 308.567 秒，新发 3 次模型请求，搜索 0 次；首次请求超时后恢复，随后合法修改被应用，但后续复核再次 504，当时探针最多恢复一次，因此以可重试的 `upstream_unavailable` 结束，没有交付。

根据实际长复核多次被 110 秒提供商预算中断的证据，单次完整证据复核预算改为 180 秒，仍由整项截止时间进一步约束；这不是把 8 分钟调查阈值改成超时限制。隔离探针也允许在同一任务的原始计时与持久化预算内最多执行四次尝试，避免第二次短暂故障就结束测试。没有重置计时、调用计数或搜索限额。[调整后的回归](../../artifacts/doctor-research-agent-2026-09-10/regression-narrative-duration-20260911.json)仍为 **365/365**。

[第六次回放](../../artifacts/doctor-research-agent-2026-09-10/workflow-replay6/result.json)耗时 212.430 秒，新发 3 次模型请求、搜索 0 次，没有超时或重排队。三次都提出并成功应用了有效修改：修复引用和章节结构，纠正出版年份范围及一处不应从含糊摘要自行推算的样本量，再修复正文与局限部分的前后矛盾。最后修改完成时旧生成预算已用尽，没有剩余调用核验新稿，所以仍未交付。

因此把完整 Agent 通路的预算改为 **身份最多 8 次、资料最多 12 次、三段生成 3 次、独立复核最多 6 次，共 29 次**。修改仍必须经下一次审核，不能把最后一次修改自动算作批准。配置预检明确检查这套分配；原有单段通路维持其独立预算。探针单次输入上限 34000、总输入 1000000，单次输出上限 17000、总输出 300000；完整复核实际单次请求上限仍为 16000。保留原有总输出硬上限，没有为了增加调用次数突破它。新增 Worker 集成测试验证三次修改之后仍有独立批准调用，且最终提交四种文件。[最终相关回归](../../artifacts/doctor-research-agent-2026-09-10/regression-narrative-budget-ready-20260911.json)为 **367/367**，构建通过；下一次真实回放待补记。

另对初稿只投影合同所需的标题、摘要、关键词与正文，防止多余模型字段进入候选后无法修复；这一机械投影在第六次回放冻结之后加入，并有回归覆盖，不把该回放作为这项后续变化的真实验证。

[新增运行时代码的有限核查](../../artifacts/doctor-research-agent-2026-09-10/generic-runtime-source-audit-20260911.json)未发现 15 个案例姓名或机构原字符串，并记录源码哈希。该检查只能说明未加入这些字面预置，不能替代原始输入盲验或证明全部泛化能力。

已校验并保存全部完成试跑的导出哈希，分别清理了 [canary3、回放 1–3](../../artifacts/doctor-research-agent-2026-09-10/cleanup-replays-20260911.json)、[回放 4–5](../../artifacts/doctor-research-agent-2026-09-10/cleanup-replays-second-20260911.json)和[回放 6](../../artifacts/doctor-research-agent-2026-09-10/cleanup-replay6-20260911.json)的临时目录与压缩包。

当前仍未完成全部 15 位真实医生的原始输入完整交付、公网准入验证和生产发布。新通路尚未证明完整服务达到 5 分钟优秀或 8 分钟可接受；诊断任务重置计时且复用历史结果，不能作为完整服务 SLA。

## 第七次回放：交付成功与内容验收分开

[第七次回放](../../artifacts/doctor-research-agent-2026-09-10/workflow-replay7/result.json)冻结提交 `b46d7c3f91c5bbb786678ff49d8e696db80b22f9` 的代码，仍使用第二次回放的原始身份、证据和初稿状态，未提供人工答案。搜索 0 次、模型调用 3 次，耗时 **271.958 秒**，实际交付基础资料、综述、问题、答案四个文件，大小和 SHA-256 均通过检查。三次复核分别耗时 156.751、81.976、32.686 秒；均无准入等待或上游超时。最后一次模型独立批准了修改后的候选稿。

但[最终正文](../../artifacts/doctor-research-agent-2026-09-10/workflow-replay7/verified-artifacts/review.md)仍将一项队列的症状性手骨关节炎人群写成“无症状”。[保存的原始摘要](../../artifacts/doctor-research-agent-2026-09-10/workflow-replay7/checkpoints.json)明确为 symptomatic HOA，核心证据表也译为症状性。第一次复核其实已指出这一问题，但 10 个补丁中的最后一项把目标哈希的一个字符抄错，整批原子修改被拒绝；下一轮只收到笼统格式错误，前轮发现没有保留，后续修订与批准漏掉了它。因此记录为 **Worker 交付成功、人工内容抽查失败**，不能声称案例通过。

由此作两项通用修复：

- 整份 `candidate_sha256` 已绑定当前完整候选、目标编号和所有原值。响应中的修改只需 `target_id` 与 `value`，不再重复要求每个段落复制 64 位原值哈希；没有模糊匹配、猜测校验值或部分应用失败批次。整稿哈希不匹配、不可编辑目标、重复目标、错误类型与超限仍拒绝；修改后仍需单独审核。错误反馈明确指向失败补丁及原因。
- 每轮有效格式的评审解释和应用结果保存在后续评审输入的 `prior_reviews` 中，包括被拒绝的补丁批次。它们是待核实线索，不能代替原文；提示要求核实并处理以前提出的问题，对认为不成立的旧问题也须说明依据。没有针对医生、机构或某篇论文添加纠错规则。

新增测试用通用段落验证：整稿变化及段落编号改变后，旧批次仍被拒绝；第二个补丁类型错误时第一个补丁不会部分应用；错误原因与之前的事实疑点在下一轮均可见。最新[相关回归](../../artifacts/doctor-research-agent-2026-09-10/regression-narrative-review-memory-20260911.json) **368/368**，构建通过。这两项修复晚于第七次回放冻结，真实模型验证待补记。

第七次回放导出包哈希 `1520a35e22d9ca8dd3cd97c69c16a96e36e39af62b2936db06e656a8eed4cf68` 已核对，临时目录和压缩包[已清理](../../artifacts/doctor-research-agent-2026-09-10/cleanup-replay7-20260911.json)。本次续作新增 SerpAPI 请求仍为 0；历史已记录探针合计为 4 次。

## 第八次回放与来源分组复核

[第八次回放](../../artifacts/doctor-research-agent-2026-09-10/workflow-replay8/result.json)冻结提交 `257cc7d801b5b6558aaae92b1e035180813bdc6d`，仍使用相同原始诊断状态，耗时 **211.170 秒**，搜索 0 次、模型调用 4 次，四个文件的哈希检查通过。三次修改全部合法应用，后续评审能继续核实前轮提出的问题，最后独立批准。这是补丁协议与复核记录的真实验证；但它仍漏掉上述症状分组表述错误，人工内容抽查失败。没有因 Worker 返回成功就登记案例通过。导出包 SHA-256 为 `1d700e66069a2bba5f1cb7df3d815643035a02a605b1fd86d6e7a806fa5dffd0`，临时产物[已清理](../../artifacts/doctor-research-agent-2026-09-10/cleanup-replay8-20260911.json)。

四轮整稿审核仍漏掉一个具体语义错误，说明仅保留历史与重复整稿审阅不足以保证细节覆盖。当前增加最多两个并行的来源分组核查，作为六次复核预算的一部分：按已选参考文献机械分组，每篇恰属一个核查任务；核查 Agent 读取完整的所属摘要和全文待编辑目标，聚焦引用这些来源的陈述。代码不判定医学语义，也不预置某篇文献的纠错答案。模型需核查否定、研究人群与分组、纳排条件、干预与对照、效应方向、终点、日期、设计、单位、分母、不确定性和证据适用范围，不能只对数字。

核查输出只能引用所属来源 ID 与已有编辑目标 ID，返回具体疑点。整稿 Agent 再结合全部来源判断、修改并接受后续独立审核；分组核查没有修改权，也不能独立批准报告。传输失败原样进入持久化恢复；并行同伴调用先完成保存，再传播失败。格式无效或越界来源不会被当成“没有问题”。两个来源核查使用最多 4000 输出 token，其他复核仍最多 16000；总调用上限仍为 29，没有延长整项截止时间。此改动晚于第八次回放，真实模型验证待补记。

构建通过，[相关回归](../../artifacts/doctor-research-agent-2026-09-10/regression-narrative-source-audits-20260911.json)为 **371/371**，包括全部来源分组覆盖、核查疑点传递、错误来源拒绝、同伴调用完成后传播上游故障，以及 Worker 实际重排队恢复与四文件交付。仍属机制验证，不替代真实医学内容验收。

[免费额度查询](../../artifacts/doctor-research-agent-2026-09-10/serpapi-balance-20260911.json)在 2026-09-10 21:50:02 UTC 确认仍剩 43 次。本次续作尚未发起新付费搜索。

## 第九次回放与原始输入沈嵘试跑

[第九次回放](../../artifacts/doctor-research-agent-2026-09-10/workflow-replay9/result.json)冻结提交 `18e7f91f1def48ac4d294c5071c33db983956445`。两个来源核查并行约 32 秒完成，随后整稿修改耗时 96.131 秒，整项 128.463 秒以 `resource_budget_exceeded` 结束，没有产物、没有搜索。来源核查发现了此前遗漏的作者局限归属、研究中心数量、样本分母、干预与对照关系、纳入标准和原文内部矛盾。整稿 Agent 处理了多项疑点，但又将先前生成的核心表当成权威，保留了原始摘要未支持的“单中心”描述。这说明“经过前轮模型复核”不能成为高于原始来源的权威标签。

用保存的响应在本地完全离线重放后，确认下一轮预计输入为 **34728 token**，超过当时单次 **34000** 的上限；并非整体输入池耗尽，也不是模型上游失败。新增 `research_resource_budget_exceeded` 内部诊断事件，记录具体限制及可用时的实际/上限值，不输出提示词或凭据，公开失败类型保持不变。

探针单次输入上限改为 **40000**，整项输入硬上限仍为 **1000000**。Agent 配置不再强制假设所有 29 次潜在调用都占满最大输入长度：检查单次容量后，每次实际输入仍由持久化资源池扣费并检查总额。模型调用数和输出预算的配置检查保留，未提高整项输出、模型次数、截止时间或搜索上限。旧通路的原有配置校验不变。这允许不同阶段按实际上下文大小共享已有总额，不保证每次调用都能用满单次上限。

[canary5](../../artifacts/doctor-research-agent-2026-09-10/workflow-canary5/result.json)仅输入沈嵘、南京市妇幼保健院和原截图职务，没有历史状态、官方 URL 或人物资料。一次 SerpAPI 请求在 **17.905 秒**成功，来源确认了姓名、医院和党委书记身份。[身份复核](../../artifacts/doctor-research-agent-2026-09-10/workflow-canary5/model-responses/resolve_identity-5.json)却要求同时证明额外的副院长任职，随后追加调查耗尽八次身份模型预算。整项 **78.602 秒**结束，无文件。它是身份消歧任务范围错误，不是医生搜不到；未计为案例通过。

对应的通用调整：身份 Agent 与复核 Agent 明确区分“确认同一个人”和“核实全部履历”。可靠来源已建立姓名、机构与兼容职业关系时，额外未核实职务作为 `identity.limitations` 传入资料阶段并保留到结果；不能将该职务写成已确认事实。真实机构、科室或人物冲突仍需核实，不能以资料缺失掩盖。没有医生或行政职务专用匹配表。

对于报告证据表，允许整稿复核 Agent 修改五类描述字段：研究设计、样本与来源、方法、结果、局限。每行的文献编号及对应关系不可修改，医生身份仍不可修改；原始摘要高于任何先前生成的解释。修订后的表格与正文必须在下一次独立调用审核，最终校验和文件渲染保留获批修改，不再用旧表覆盖。来源分组核查也能看到这些描述字段及其固定的文献映射。没有直接把本次抽查结论写入运行时规则。

构建通过，[相关回归](../../artifacts/doctor-research-agent-2026-09-10/regression-agent-scope-and-resource-20260911.json)为 **376/376**。新增覆盖包括身份限制传递、单次输入超限在请求/扣费前被拒绝且可诊断、共享输入池配置、核心表描述修正的后续复审及最终四文件保存、禁止修改文献身份。这些后续改动的真实模型验证尚待补记。

探针恢复 SerpAPI 的正常缓存行为，不再强制 `no_cache=true`。全新请求仍只含原始三个字段、不导入本地历史资料；服务端搜索缓存不等于测试人员预置医生答案。每次探针的网络搜索请求仍有硬上限，实际计费需结合免费账户查询和请求记录区分。

上述两次试跑导出哈希分别为 `f2bbc76c3b48f13b25c76c65b3552bda28f00c3f1c49b8b856ffbec480333dee`、`51ec17bf7b7361f68e21fb3aabffca85e71c2eb0dbac8a582a4b3496dc1e1d55`，已核对并[清理远端临时产物](../../artifacts/doctor-research-agent-2026-09-10/cleanup-replay9-canary5-20260911.json)。本次续作新增搜索 **1 次**；已记录探针历史合计 **5 次**。全部 15 位原始案例完整服务验收仍待完成，生产尚未部署。

## 身份通过后的文献纠错与输出容量（22:28 UTC 后）

[canary6](../../artifacts/doctor-research-agent-2026-09-10/workflow-canary6/result.json)仍只提供沈嵘的原始姓名、医院和职务，未导入历史材料。第六次身份调用通过独立复核，随后进入真实 PubMed 调查。整项 **347.924 秒**以文献阶段资源预算耗尽结束，无产物；耗时在可接受区间内，但失败任务不能算服务达标。

文献 Agent 第八次调用已提交 19 篇领域论文与 7 条核心证据，之后连续受到原文引句、字段格式和 JSON 尾随内容检查阻断。第 8、10、11、12 次均生成长提案，分别耗时约 53.5、43.9、87.7、37.5 秒；没有一次抵达独立证据复核。不能将此解释成医生或论文搜不到。每次只纠正一个引句却要求重写完整提案，放大了格式错误和等待时间。

[replay10](../../artifacts/doctor-research-agent-2026-09-10/workflow-replay10/result.json)耗时 **276.593 秒**，两路来源核查中一路再次发现了有症状人群被写成无症状的问题。随后整稿复核请求 `req-63a53d5e-2ca3-4c7b-9ebe-571f8f49eeca` 在 **163.950 秒**后收到 HTTP 413 / `output_exhausted`，没有完整响应或文件。旧分类将它返回为不可重试的 `upstream_unavailable`；它是输出容量问题，并非已经证实的服务中断。

本次通用改动：

- 原文页和已读论文提供稳定的 `passageId`。Agent 选择原文片段后，代码插入该来源的确切文本；独立模型仍判断原文是否支持事实。本人论文可用选定作者的 `affiliationIndex`，代码不能借用其他作者的单位。保留直接引用兼容，未自动把近似引句当作准确原文。
- 未获批提案保存在本任务状态中，允许按当前提案哈希提交 JSON pointer 局部替换。仅能替换现有值，拒绝过期哈希、路径冲突、原型属性和超限内容。修正后重新校验完整提案并独立复核，不能局部放行。
- 报告允许对字符串目标做唯一、精确且互不重叠的 `find/replace`，也可单独修改某个问题、答案文本或引用集合。所有编辑仍绑定完整候选哈希。提示中不重复整组问答，减少输入与修改输出。
- 整稿复核的单次输出耗尽可以使用剩余复核调用额度重新处理，明确要求精简解释和局部修改；不采用截断的响应、不增加整项调用预算或时限。最终输出容量失败归为资源问题并留下诊断，而非笼统的上游不可用。

构建通过，[相关回归 389/389](../../artifacts/doctor-research-agent-2026-09-10/regression-agent-local-edits-20260911.json)。测试覆盖原文片段解析与后续语义审核、恢复后局部修正、无效编辑的原子拒绝、局部问答修正、输出耗尽后的有限恢复。它们验证机制，真实模型与完整案例验收仍待完成。

canary6 的一个搜索 HTTP 请求耗时 **976 毫秒**，返回与 canary5 相同的服务商搜索 ID；22:28:29 UTC 免费 Account API 仍为 **剩余 42、已用 208**，本轮未新增计费。历史累计 **6 个搜索 HTTP 请求**，不要把该计数直接当成计费次数。两次完成任务的导出 SHA-256 已校验、敏感字段扫描通过，并[清理远端临时产物](../../artifacts/doctor-research-agent-2026-09-10/cleanup-replay10-canary6-20260911.json)。生产未部署，15 位原始案例仍未完成完整服务验收。

## 本人论文的履历引用被代码误拒（22:50 UTC 后）

[canary7](../../artifacts/doctor-research-agent-2026-09-10/workflow-canary7/result.json)在约 13 秒内通过四次身份调用，读取 19 篇论文。Agent 第六次证据调用提交提案，后面六次实际使用了局部修改，单轮纠错输出多数降至数百 token。但仍因预算耗尽失败，整项 **207.430 秒**、没有文件。

继续核对调用位置而非只看错误文案后，确认前述“引句不匹配”中有代码误判：`validateEvidence` 对所有履历事实调用仅允许网页的引用函数，导致 Agent 引用已经读到的本人论文来说明研究方向或代表作时，准确的论文标题、摘要原句和有效片段编号均被拒绝。这不是引用重写能够修复的问题。未知/不允许的来源与实际引句缺失此前还共用误导性文案。

现在允许履历事实引用已读论文，由独立模型结合作者及其本人单位判断归属；不按事实关键词建立特殊案例表。复核输入补入所有履历事实引用的论文（即使未列入领域文献），并提供期刊、完整摘要和所需作者单位元数据。最终来源清单、履历声明和覆盖说明保留这些 PubMed 来源。用于证明缺失作者单位的外部发表目录仍必须独立于论文自身，不能循环佐证。

[replay11](../../artifacts/doctor-research-agent-2026-09-10/workflow-replay11/result.json)耗时 **221.963 秒**，两次来源核查、两次有效局部修改后，随后两次响应均为当前候选的独立接受，但省略了空 `replacements` 数组，严格格式检查因此拒绝。改为接受/拒绝决策缺少该字段时按无修改处理；修订仍要求明确替换内容，未知字段、未通过检查和过期哈希继续拒绝。格式错误反馈指出具体缺失或非法字段。

完全离线复用 replay11 的五个原始响应（不改正文、不补答案、不发模型请求）后，[诊断渲染](../../artifacts/doctor-research-agent-2026-09-10/workflow-replay11-recorded-responses/result.json)完成四种文件及哈希检查，说明格式误拒已消除。人工抽查确认先前有症状/无症状错误已改正；仍发现第三条核心证据及正文含摘要未支持的“单中心”，第四条纳入条件的摘要内部歧义未充分处理，故内容验收仍标为失败。不能把这次离线文件生成当成新案例完整通过。

构建通过，[回归 394/394](../../artifacts/doctor-research-agent-2026-09-10/regression-agent-paper-facts-20260911.json)。新增覆盖本人论文事实引用、领域列表以外的履历来源及错误归属的独立拒绝、最终四文件的来源闭合、接受响应省略空编辑字段与未知编辑字段拒绝。最新免费账户核查为 22:50:29 UTC，仍剩余 **42**、已用 **208**；canary7 的搜索请求 445 毫秒返回同一服务商缓存 ID，无新增计费。累计搜索 HTTP 请求 **7 次**。完成的远端任务已[核对并清理](../../artifacts/doctor-research-agent-2026-09-10/cleanup-replay11-canary7-20260911.json)。

整体职责划分和待验收边界另见[设计审视](doctor-research-design-review-2026-09-11.zh-CN.md)。生产仍未部署，原始案例完整服务验收仍未完成。
