# MedEvidence 客户端：Qwen-Image-2.1 默认生图实测通知

交接方式：供用户转发；未向客户端团队直接发送。以下为可转发正文。

---

MedEvidence 团队：默认生图已于 2026-09-22 北京时间 09:39 切换为本地部署的 **Qwen-Image-2.1**，LLaDA-Image-Turbo-FP8 保留为第一后备，请安排客户端内部实测。

接入方式保持不变：

- 地址：`POST https://goldencode.instmarket.com.au:1443/gateway/images/generations`
- 模型：`medcode-image-default`
- 沿用现有登录、API key 和生图流程，无需把客户端模型名改成 `qwen-image-2.1`。

服务端已通过公网真实生图验收：HTTP 200，实际命中 `qwen-image / qwen-image-2.1`，1024×1024 JPEG 用时 **57.3 秒**，中文标题“每天运动，健康生活”和三个运动图标正常。另一个 Qwen 原生 1536×1024 横图样本用时 **76.7 秒**。普通文本请求也通过。以上是样本耗时，请保留现有 210 秒超时；60 秒进度提示出现时继续等待，不要自动重复提交。

请从实际安装的客户端验证：

1. 中文健康教育海报、中英文混排标签、指定数量图标的生成质量。可复测“白底青绿色健康海报，标题准确写‘每天运动，健康生活’，下方依次放步行、骑行、游泳三个图标”。
2. 方图 1024×1024、横图 1536×1024、竖图 1024×1536，以及 JPEG、PNG、WebP 的生成、显示、打开、保存与 PPT 插图流程。
3. 等待进度、超时或失败提示是否正常；避免重复扣费或重复提交。模型切换后首次请求及连续请求均记录耗时。

请回传客户端版本、测试时间、提示词、尺寸/格式、总耗时、request ID，以及图片或错误截图。不要发送 API key。实际是否命中 Qwen 或后备模型由 Gateway 根据 request ID 核对。

服务端验收 request ID：`req-cbbcf0dd-b456-49a2-84f1-13c74c53031b`。本次验证范围为现有文生图能力；客户端实测结果待回传。
