[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "star-gpu-scheduler-client"
version = "0.1.0"
description = "Private single-host resource scheduling for Qwen and RADAR"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["star_gpu_scheduler*"]
